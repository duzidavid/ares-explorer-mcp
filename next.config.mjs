/**
 * API-only Next.js app: the sole route is the MCP endpoint at /api/mcp
 * (app/api/mcp/route.ts). No pages, no React UI — the MCP App's view ships as
 * the inlined ui:// HTML resource, not as a Next page.
 *
 * @type {import('next').NextConfig}
 */
const nextConfig = {};

export default nextConfig;
